{
  "name": "browser-strike",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "vite build",
    "preview": "vite preview --port 4173",
    "party": "vite build && partykit dev",
    "deploy": "vite build && partykit deploy"
  },
  "dependencies": {
    "partysocket": "^1.3.0",
    "three": "^0.186.0"
  },
  "devDependencies": {
    "partykit": "^0.0.115",
    "vite": "^8.3.0"
  }
}
